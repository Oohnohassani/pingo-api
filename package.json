{
  "name": "pingo-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server data/places.json --port $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.13"
  }
}
